const { Router } = require('express');
const express = require('express');
const userssController = require('../controllers/users-controller');

const router = express.Router();

router.get('/', );

router.post('/signup',);

router.post('/login',);

module.exports = router;